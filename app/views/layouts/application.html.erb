<!DOCTYPE html>
<html>
  <head>
    <title>Zblog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= javascript_include_tag 'application' %>
  </head>

  <body>

    
      <% if user_signed_in? %>

        <div class="flex flex-col mx-auto p-2 max-w-4xl">
          <% if current_user.user_profile.profile %>
            <%= full_name(current_user.user_profile.profile) %>
          <% else %>
            <%= current_user.email %>
          <% end %>
        </div>

      <% else %>

        <%# https://github.com/heartcombo/devise/issues/5236 %>
        <%= button_to "Log in", user_azure_activedirectory_v2_omniauth_authorize_path, method: :post, "data-turbo": false, class: "bg-blue-500 rounded text-white p-2" %>

      <% end %>

        <div class="flex flex-col mx-auto p-2 max-w-4xl">
          <% if user_signed_in? %>
            <%= yield %>
          <% end %>
        </div>
      
    
  </body>
</html>
