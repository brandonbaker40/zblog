<!DOCTYPE html>
<html>
  <head>
    <title>Zblog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= javascript_include_tag 'application' %>
    <script src="https://kit.fontawesome.com/bb651c462b.js" crossorigin="anonymous"></script>
  </head>

  <body>
    <div class="flex flex-col mx-auto p-2 max-w-4xl">
        <% if user_signed_in? %>
          
          <% if current_user.user_profile.profile %>
            <%= full_name(current_user.user_profile.profile) %>
          <% else %>
            <%= current_user.email %>
          <% end %>

          <%= render "layouts/navbar" %>
          <%= yield %>

      <% else %>

        <%= render "layouts/login" %>

      <% end %> 
    </div>
  </body>
</html>
